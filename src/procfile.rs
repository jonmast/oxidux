use std::collections::HashMap;

pub fn parse_procfile_in_dir(_directory: String) -> HashMap<String, String> {
    let mut map = HashMap::new();
    map.insert("TODO".to_string(), "TODO".to_string());

    map
}
